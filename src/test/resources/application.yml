spring:
  profiles: test

  jpa:
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    org.hibernate.SQL: debug

property:
  image:
    url: /Users/joohyuk/Pictures/

app:
  host: http://localhost:8080


